## component lifecycle
componentDidMount(){
    //initialize
}
componentDidUpdate(){
    //state updated
}
componentWillUnmount(){
    //destroyed
}
A component is added to the ui or mounted (only happens once )
reactive data on the component can change or its updated (can happen multiple times )
the component will at some point be removed from the ui /unmounted